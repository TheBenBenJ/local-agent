#!/usr/bin/env python3
"""Point d'entrée du serveur MCP stdio du local-agent."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from local_agent.mcp import main  # noqa: E402

if __name__ == "__main__":
    main()
