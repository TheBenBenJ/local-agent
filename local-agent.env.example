# Surcharges locales du local-agent.
# Copier en local-agent.env (ignoré par git) pour un réglage personnel.
# Les variables déjà présentes dans l'environnement ont priorité sur ce fichier.

# Serveur local compatible OpenAI : mlx-serve, Ollama, llama.cpp, LM Studio, vLLM…
# Les anciens noms MLX_* restent lus en rétrocompatibilité.
LOCAL_LLM_BASE_URL=http://127.0.0.1:11234/v1
# auto = premier modèle chargé côté serveur
LOCAL_LLM_MODEL=auto
LOCAL_LLM_PROVIDER=mlx
LOCAL_LLM_TEMPERATURE=0
LOCAL_LLM_TIMEOUT=300
LOCAL_LLM_MAX_TOKENS=1600
# 0 = lire context_length côté serveur
LOCAL_LLM_MAX_CONTEXT=0
# Renseigner uniquement si le serveur exige un jeton Bearer
LOCAL_LLM_API_KEY=

# Bornes de contexte et de sortie
LOCAL_AGENT_MAX_FILES=40
LOCAL_AGENT_MAX_FILE_SIZE=120000
LOCAL_AGENT_MAX_OUTPUT_TOKENS=900
LOCAL_AGENT_CHUNK_CHARS=12000
LOCAL_AGENT_MAX_CHUNKS=8
LOCAL_AGENT_MAX_MATCHES=200
LOCAL_AGENT_FIX_MAX_FILE_SIZE=40000
LOCAL_AGENT_COMMAND_TIMEOUT=900
LOCAL_AGENT_COMPOUND_TURNS=25
LOCAL_AGENT_VISION=1
LOCAL_AGENT_ENABLE_VISION=1
LOCAL_AGENT_ENABLE_OCR=1
LOCAL_AGENT_ENABLE_CACHE=1

# Boucle local_task
LOCAL_AGENT_MAX_STEPS=12
LOCAL_AGENT_MAX_TOOL_CALLS=24
LOCAL_AGENT_MAX_RUNTIME=180
LOCAL_AGENT_OUTPUT_BUDGET=1500
LOCAL_AGENT_LOCAL_CONTEXT_BUDGET=48000
LOCAL_AGENT_AUTONOMY=read_only
LOCAL_AGENT_MAX_RETRIES=2

# Jira / Confluence optionnels, jamais commités. Préférer le .claude/.env.local du dépôt visé
# (JIRA_URL, JIRA_USERNAME, JIRA_API_TOKEN : déjà le format des skills lysi).
# JIRA_BASE_URL=
# JIRA_TOKEN=
# JIRA_EMAIL=
# CONFLUENCE_BASE_URL=
# CONFLUENCE_TOKEN=
